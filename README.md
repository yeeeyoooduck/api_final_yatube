## Проект «API для Yatube»

**Yatube** — это социальная сеть, которая получила новое расширение возможностей благодаря проекту «API для Yatube». Этот интерфейс предоставляет пользователям функционал для управления публикациями и подписками через API.

### Основные возможности

- Создание, просмотр, редактирование и удаление публикаций.
- Работа с комментариями: добавление, изменение, удаление и получение списка.
- Просмотр списка сообществ и информации о каждом из них.
- Управление подписками: возможность подписаться на интересующего автора и отслеживать обновления.
- Авторизация через JWT: получение, проверка и обновление токенов.

### Технологии

- **[Python](https://www.python.org/):** язык программирования, на котором разработан проект.
- **[Django](https://www.djangoproject.com/):** мощный фреймворк для создания веб-приложений.
- **[Django REST Framework](https://www.django-rest-framework.org/):** инструмент для построения RESTful API.
- **[Simple JWT](https://django-rest-framework-simplejwt.readthedocs.io/en/latest/):** библиотека для работы с JWT-аутентификацией в Django.

### Как запустить проект

1. Клонируйте репозиторий и перейдите в его директорию:

    ```bash
    git clone https://github.com/yeeeyoooduck/api_final_yatube.git
    cd api_final_yatube
    ```

2. Создайте и активируйте виртуальное окружение:

    ```bash
    python -m venv env
    source env/Scripts/activate
    ```

3. Установите зависимости из файла `requirements.txt`:

    ```bash
    pip install -r requirements.txt
    ```

4. Выполните миграции:

    ```bash
    python manage.py migrate
    ```

5. Запустите сервер разработки:

    ```bash
    python manage.py runserver
    ```

### Документация

После запуска сервера проектная документация будет доступна по адресу:  
`http://127.0.0.1:8000/redoc/`
